PREFIX dct: <http://purl.org/dc/terms/>
PREFIX ext: <http://mu.semte.ch/vocabularies/ext/>
DELETE {
  GRAPH ?g {
      ?s dct:modified ?mod.
  }
} WHERE {
  GRAPH ?g {
    ?s dct:modified ?mod.
    FILTER NOT EXISTS {
      ?s a ?thing.
    }
  }
  ?g ext:ownedBy ?someone.
}