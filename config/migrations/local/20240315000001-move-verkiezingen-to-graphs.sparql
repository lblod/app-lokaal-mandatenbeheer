PREFIX ext: <http://mu.semte.ch/vocabularies/ext/>
INSERT {
	GRAPH ?g {
		?s ?p ?o
	}
}
WHERE {
	GRAPH <http://mu.semte.ch/graph/temp/load> {
		?s ext:inGraph ?g.
		?s ?p ?o.
		FILTER (?p != ext:inGraph)
	}
}
